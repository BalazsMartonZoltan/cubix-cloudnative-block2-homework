services:
  backapp:
    build: ./backapp
    expose:
      - "8081"
    environment:
      SERVER_PORT: 8081

  frontapp:
    build: ./frontapp
    ports:
      - "8080:8080"
    environment:
      BACK_URL: http://backapp:8081
    depends_on:
      - backapp